{
  "name": "otterday-site",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test test/*.test.mjs",
    "start": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "marked": "^17.0.0",
    "prettier": "^3.6.2"
  }
}
